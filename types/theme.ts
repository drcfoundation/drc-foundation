export interface Theme {
  foreground: string;
  text: string;
  background: string;
  border: string;
  headerFooterBackground: string;
  tooltipBackground: string;
  imageLoadingBackground: string;
}
